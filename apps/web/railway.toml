# Deploy do frontend (web): Root Directory = apps/web
# Variáveis obrigatórias: NEXT_PUBLIC_API_URL (URL do backend no Railway)

[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
