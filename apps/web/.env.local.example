# Padrão do app: produção (Railway). Só defina para dev local:
# NEXT_PUBLIC_API_URL=http://localhost:3001
# Produção (já é o padrão no código):
# NEXT_PUBLIC_API_URL=https://botao-do-panico-production.up.railway.app
