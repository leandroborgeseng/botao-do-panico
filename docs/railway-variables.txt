# Variáveis para o BACKEND (Root Directory = apps/backend)
# Serviço botao-do-panico → Variables → New Variable

DATABASE_URL=<referência ao Postgres ou postgresql://user:senha@host:porta/railway>
JWT_SECRET=<gere com: openssl rand -base64 32>
CORS_ORIGINS=*

# Variáveis para o FRONTEND (Root Directory = apps/web)
# Serviço web → Variables (obrigatório para o front chamar o backend)

NEXT_PUBLIC_API_URL=https://botao-do-panico-production.up.railway.app
